.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Local agent/tooling configuration. Never belongs in a public repository.
.mcp.json
.claude/

# Local signing identity. See Signing.xcconfig.
Local.xcconfig

# Export options carry a team id. RELEASING.md says to write one outside the tree; this
# is the guard for when someone writes it here anyway.
ExportOptions.plist
UploadOptions.plist

# Built from tools/tap.swift, per RELEASING.md's "type with it" step. A compiled binary,
# not a source file.
/tap
