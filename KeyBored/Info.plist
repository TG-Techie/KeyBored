<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Export compliance, declared by Jonah on 2026-09-05: the app uses no encryption.
       It contains no cryptography and does no networking at all — no CryptoKit, no
       CommonCrypto, no Security framework, no URLSession, no third-party dependency, and
       the keyboard has no Full Access so iOS would not let it reach the network anyway.
       Answering it here rather than in App Store Connect is why the question stops being
       asked on every upload. Do not delete this as unexplained: without it a build uploads
       successfully and is then never distributed to any tester, which is a failure that
       looks like success. -->
  <key>ITSAppUsesNonExemptEncryption</key>
  <false/>
  <!-- Also the brand, from the same single source as CFBundleDisplayName. Some Settings
       surfaces fall back to CFBundleName, which is why leaving it as the target name
       showed the old name on a real phone even once the display name was right. -->
  <key>CFBundleName</key>
  <string>$(PRODUCT_DISPLAY_NAME)</string>
  <key>UILaunchScreen</key>
  <dict/>
  <key>UIApplicationSceneManifest</key>
  <dict>
    <key>UIApplicationSupportsMultipleScenes</key>
    <false/>
  </dict>
  <!-- All four, because the app ships to iPad as well as iPhone and iPad multitasking
       requires every orientation. Omitting this key entirely builds and runs fine and is
       rejected at upload: App Store Connect returns error 90474, "No orientations were
       specified in the com.tg-techie.app.keybored bundle". The container app is a single
       informational screen, so it has no reason to refuse any of them. -->
  <key>UISupportedInterfaceOrientations</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
    <string>UIInterfaceOrientationPortraitUpsideDown</string>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
  </array>
</dict>
</plist>
